@model IEnumerable<neighborhoodStore3.Models.Sales>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PruductID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClientType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PruductID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClientType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalesID })
        </td>
    </tr>
}

</table>
